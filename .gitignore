# Dependencies
node_modules/

# SvelteKit / Vite (Client Build Artifacts)
.svelte-kit/
/build/
/client/.svelte-kit/
/client/build/
/client/package/

# Environment Variables (Keep secrets out of the repo)
.env
.env.*
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# System Files
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Testing / Coverage
coverage/
.nyc_output/

# Optional: Ignore the profile data locally if you are doing dev work
# and don't want to accidentally commit your test profile to the 'main' template.
# (Uncomment if you are the maintainer, but Users NEED to commit this).
# /profile/profile.json
# /profile/assets/*
# !/profile/.keep
